SET  
   1-1. HashSet
   			 不能保证元素的排列的顺序
   			 线程安全的问题
   			 集合元素可以是null
   			   注意:如在进行判断两个元素是否相等的情况
   			   		特别是自定义对象的时候当成参数传入到集合中的
   			   		进行比对，需要重写HashCode和equals
        LinkedHashSet
        	 插入的性能没HashSet高,
        	 不允许集合元素重复，是唯一的
        	 保证了怎么存入就怎么进行取出
        TreeSet
        	一样唯一
        	  排序的时候选进行自然的排序和自定义排序
        	默认是  自然的排序
        	自定义排序需要进行编写compare(t 1
        	,t 2)
        map
        	key-value(映射关系的数据)
        	K不能重复,v课重复
        HashMap
        	K和V可以使用null